<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-md-12">
    <h1>Команды участники Орловской Волейбольной Лиги</h1>
  </div>
</div>
<% @teams.each do |team| %>
<div class="row">
  <div class="col-md-11">
        <h3><%= link_to team.title, team  %></h3>
  </div>
  <div class="col-md-1">
        <% if is_admin? %>
        <%= link_to 'Edit', edit_team_path(team) %></td>
        <%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %>
        <% end %>
   </div>
</div>
<% end %>
